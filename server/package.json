{
  "name": "routine-app-api",
  "private": true,
  "type": "module",
  "description": "루틴 앱 동기화 API — 의존성 0개(Node 내장 모듈만). native 모듈을 피해 arm64 이미지 빌드가 QEMU를 타지 않게 한다.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "DEV_NO_AUTH=1 node --watch src/index.js"
  }
}
